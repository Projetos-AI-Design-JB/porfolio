# Plan: Skeleton Clone

1. **Create Stylesheet:** Copy `skeleton.css` to `skeleton-clone.css`.
2. **Create Page Component:** Copy `page.tsx` from `skeleton` to `skeleton-clone`.
3. **Update Imports:** Ensure the new page imports `skeleton-clone.css`.
4. **Update Assets:** Change `<img src="/jo-mendes.png">` to `<img src="/ethereal/images/hero.png">`.
5. **Update Copy:** Modify text elements to match the new Ethereal theme.
6. **Refinements:** Add `aria-current="page"` to the active nav link.